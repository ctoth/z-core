# UM0050 verification log

| Verified fact | UM0050 section or table | Date |
|---|---|---|
| Manual title: *Z8018x Family MPU User Manual* | Cover, UM005004-0918 | 2026-07-20 |
| Manual revision: UM005004-0918 (September 2018) | Cover and document footer | 2026-07-20 |
| Official source: https://www.zilog.com/docs/z180/um0050.pdf | Zilog documentation listing for UM0050 | 2026-07-20 |
| CPU register set comprises AF, BC, DE, HL, alternate AF/BC/DE/HL, I, R, IX, IY, SP, and PC | Software Architecture, CPU Registers, Figure 74, pp. 175–177 | 2026-07-20 |
| R bits 0–6 increment on every CPU opcode-fetch (M1) cycle; R resets to 00h | Software Architecture, R Counter (R), p. 177 | 2026-07-20 |
| S reflects result bit 7, Z reflects a zero result, H records low-nibble carry/borrow, P/V is parity for logical results or signed overflow for arithmetic, N distinguishes subtraction from addition, and C records high-bit carry/borrow | Software Architecture, Flag Register, pp. 178–179 | 2026-07-20 |
| Register codes 000–101 and 111 select B, C, D, E, H, L, and A respectively | Instruction Set, Table 32, p. 207 | 2026-07-20 |
| Unprefixed ADD/ADC/SUB/SBC/CP set S, Z, H, arithmetic overflow in P/V, N according to add/subtract, and C; AND sets H and parity while resetting N/C; OR/XOR reset H/N/C and set parity | Arithmetic and Logical Instructions (8-bit), Table 38, pp. 211–216 | 2026-07-20 |
| Unprefixed INC/DEC update S, Z, H, and overflow, leave C unchanged, and respectively reset/set N; 16-bit INC/DEC leave every flag unchanged | Arithmetic Instructions, Tables 38 and 40, pp. 213, 221 | 2026-07-20 |
| RLCA/RRCA/RLA/RRA rotate A as named, update C from the shifted-out bit, reset H and N, and leave S, Z, and P/V unchanged | Rotate and Shift Instructions, Table 39, pp. 216–217 | 2026-07-20 |
| CB RLC/RRC/RL/RR/SLA/SRA/SRL operate on the selected register or (HL), set S/Z and parity from the result, reset H/N, and set C from the shifted-out bit | Rotate and Shift Instructions, Table 39, pp. 216–219 | 2026-07-20 |
| CB SET and RES modify only the selected bit and leave every flag unchanged; CB BIT sets H, resets N, preserves C, sets Z from the tested bit, and marks S and P/V undefined | Rotate and Shift Instructions, Table 39, pp. 219–220 | 2026-07-20 |
| Free choice: for UM-undefined CB BIT S/P/V results, z-core sets S only when testing a set bit 7 and mirrors Z into P/V, providing deterministic conventional behavior compatible with the standard SST corpus | UM0050 Table 39 leaves these results undefined; z-core deterministic policy | 2026-07-20 |
| ADD HL,ww adds the selected 16-bit pair into HL, updates H and C, resets N, and leaves S, Z, and P/V unchanged | Arithmetic Instructions (16-bit), Table 40, p. 221 | 2026-07-20 |
| The 01dddsss block encodes LD r,r', LD r,(HL), and LD (HL),r; these operations do not alter flags | Data Transfer Instructions, Table 41, pp. 222–223 | 2026-07-20 |
| All unprefixed 8-bit and 16-bit load forms in Tables 41–42 leave flags unchanged; 16-bit memory values are stored and loaded low byte at the named address and high byte at address plus one | Data Transfer Instructions, Tables 41–42, pp. 222–225 | 2026-07-20 |
| PUSH stores the pair low byte at SP-2 and high byte at SP-1 before setting SP to SP-2; POP restores low from SP and high from SP+1 before adding two to SP; flags are unchanged except that POP AF restores F from the stack | Stack and Exchange, Table 44, pp. 227–228 | 2026-07-20 |
| EX AF,AF', EXX, EX DE,HL, and EX (SP),HL perform the named exchanges and leave flags unchanged except for exchanging F as part of AF | Stack and Exchange, Table 44, p. 228 | 2026-07-20 |
| Unprefixed CALL, JP, JR, DJNZ, RET, conditional forms, and RST use the documented conditions and stack ordering and do not alter flags | Program Control Instructions, Table 45, pp. 229–230 | 2026-07-20 |
| IN A,(n) and OUT (n),A form a 16-bit port with n on A0–A7 and A on A8–A15; IN replaces A, OUT writes A, and neither changes flags | I/O Instructions, Table 46, pp. 231–232 | 2026-07-20 |
| DAA adjusts BCD addition/subtraction using H's low-nibble carry/borrow indication; it updates S, Z, H, parity, and C while preserving N | Software Architecture, Flag Register, p. 179; Special Control Instructions, Table 47, p. 235 | 2026-07-20 |
| CPL complements A, sets H and N, and leaves S/Z/P/V/C unchanged; SCF sets C and resets H/N; CCF complements C, copies old C into H, and resets N | Arithmetic and Logical Instructions, Table 38, p. 212; Special Control Instructions, Table 47, p. 235 | 2026-07-20 |
| DI clears IEF1/IEF2, EI sets IEF1/IEF2, and interrupts are not sampled at the end of either instruction; both leave flags unchanged | Special Control Instructions, Table 47 and note 7, p. 235 | 2026-07-20 |
| NOP is opcode 00h and HALT is opcode 76h; neither alters flags | Special Control Instructions, Table 47, p. 235; Op Code Map, Table 48, p. 247 | 2026-07-20 |
| Hardware RESET restarts execution at logical and physical address 00000h | Operation Modes, RESET Timing, Figure 15, p. 25 | 2026-07-20 |
| I resets to 00h | Software Architecture, Interrupt Vector Register (I), p. 177 | 2026-07-20 |
| Free choice: CPU registers without a UM0050 reset value are initialized to 0000h for deterministic emulation | UM0050 specifies PC/I/R reset values but no reset value for the remaining CPU registers; z-core deterministic policy | 2026-07-20 |
| IEF1 and IEF2 reset to 0 | Interrupt Sources During RESET, p. 83 | 2026-07-20 |
| DD/FD are defined only where Table 48 substitutes IX/IY for an HL or (HL) operand; JP (HL) is substituted, prefixed EX DE,HL is illegal, and every other DD/FD second opcode is undefined rather than prefix-ignored and therefore generates TRAP | Op Code Map, Table 48 notes, pp. 247–248; TRAP Interrupt and Figure 32, pp. 70–71 | 2026-07-20 |
| CB opcodes 30h–37h have no defined SLL operation on Z80180 | Op Code Map, Table 49, p. 249 | 2026-07-20 |
| DDCB/FDCB apply Table 48's (HL) substitution only to Table 49's g=(HL) cells, so documented final opcodes have low bits 110 and operate on (IX/IY+d); register-result forms and SLL are undefined and generate TRAP | Op Code Map, Table 48 Note 3 and Table 49 Note 1, pp. 248–249; TRAP Interrupt and Figure 33, pp. 70–72 | 2026-07-20 |
| The defined ED opcode set is exactly the populated cells in the Z80180 ED map; blank ED cells are undefined | Op Code Map, Table 50, p. 250; TRAP Interrupt, pp. 70–72 | 2026-07-20 |
| ED ADC/SBC HL,ww update S/Z/H/overflow/N/C from the 16-bit result, while ED 16-bit loads leave flags unchanged | Arithmetic Instructions (16-bit), Table 40, p. 221; 16-Bit Load, Table 42, p. 224 | 2026-07-20 |
| NEG computes zero minus A and updates S/Z/H/overflow/N/C as subtraction; RLD/RRD update S/Z/parity from the resulting A, reset H/N, and preserve C | Arithmetic and Logical Instructions (8-bit), Table 38, p. 213; Rotate and Shift Instructions, Table 39, pp. 217–218 | 2026-07-21 |
| LDI/LDD/LDIR/LDDR preserve S/Z/C, reset H/N, and set P/V iff the decremented BC is nonzero; CPI/CPD/CPIR/CPDR set S/Z/H from A minus memory, set N, preserve C, and set P/V iff the decremented BC is nonzero | Block Transfer, Table 43, pp. 225–226 | 2026-07-21 |
| ED IN g,(C) updates S/Z/parity from the input, resets H/N, and preserves C, with g=110 changing flags only; ED OUT (C),g leaves flags unchanged | I/O Instructions, Table 46, pp. 231–232 | 2026-07-20 |
| INI/IND/INIR/INDR perform the input through initial BC and then decrement B; OUTI/OUTD/OTIR/OTDR use decremented B on address lines A8–A15 with C on A0–A7 for the output transfer | I/O Instructions, Table 46, pp. 231–234; pinned standard A2/A3 port cases | 2026-07-21 |
| Free choice: for standard block-I/O S/H/P/V/C results that UM0050 marks undefined, z-core uses the conventional standard-corpus rule: S/Z/X/Y from decremented B, N from the transferred byte, H/C from overflow of byte plus C adjusted in the transfer direction for input or updated L for output, and P/V from parity of the sum's low three bits XOR decremented B. A nonterminal repeat applies the corpus-pinned H/P/V correction based on the carry flag, N, and decremented B | UM0050 Table 46 leaves these results undefined, pp. 231–234; standard SST ED A2/A3/AA/AB/B2/B3/BA/BB transitions | 2026-07-21 |
| RETI pops PC and leaves IEF1/IEF2 unchanged; RETN pops PC and then copies IEF2 into IEF1. Standard v1 `ed 4d` instead expects RETI to copy IFF2 into IFF1, so that file contradicts the Z180 transition and is excluded | Program Control Instructions, Table 45, p. 230; focused standard SST execution | 2026-07-21 |
| ED IM0/IM1/IM2 select the named interrupt mode and leave flags unchanged; SLP enters sleep and leaves flags unchanged | Special Control Instructions, Table 47, p. 235 | 2026-07-20 |
| An undefined first, second, or third opcode fetch invokes TRAP; UFO distinguishes second/third-opcode cases | TRAP Interrupt and Figures 32–33, pp. 70–72 | 2026-07-20 |
| IN0 includes ED30 as the `g=110` flags-only form; ED00/08/10/18/20/28/38 load B/C/D/E/H/L/A, while OUT0 has only the seven register forms and ED31 is not documented | I/O Instructions, Table 46, pp. 231–232; ED op-code map, Table 50, p. 250 | 2026-07-20 |
| The flag-table symbols mean not affected (bullet), affected (up arrow), undefined (`X`), set (`S`), reset (`R`), parity (`P`), and overflow (`V`) | Instruction Set, flag notation, p. 209 | 2026-07-20 |
| MLT BC/DE/HL/SP multiplies the unsigned high and low bytes of the selected pair into that 16-bit pair and does not affect flags; encodings are ED4C/5C/6C/7C | CPU Control Instructions, Table 38, p. 213; ED op-code map, Table 50, p. 250 | 2026-07-20 |
| TST forms compute A AND register, (HL), or immediate data without changing either operand; S and Z follow the result, H is set, P/V is result parity, and N and C are reset | Exchange, Block Transfer, Search, and Test Instructions, Table 40, pp. 215–216 | 2026-07-20 |
| IN0 reads port 00m into the selected register, with ED30 changing flags only; S and Z follow the byte, H and N reset, P/V is parity, and C is unaffected | I/O Instructions, Table 46, p. 231 | 2026-07-20 |
| OUT0 writes the selected register to port 00m and does not affect flags | I/O Instructions, Table 46, p. 232 | 2026-07-20 |
| TSTIO reads port 00C and ANDs it with the immediate byte without changing the port; S and Z follow the result, H is set, P/V is parity, and N and C reset | I/O Instructions, Table 46, p. 233 | 2026-07-20 |
| OTIM and OTDM write (HL) to port 00C, adjust HL and C in the named direction, and decrement B; Z is defined by B becoming zero and N by the output byte's most-significant bit, while S, H, P/V, and C are marked affected without a resulting-value rule | I/O Instructions, Table 46, pp. 232–234, notes 5–6 | 2026-07-20 |
| Terminal OTIMR and OTDMR set Z and P/V, reset S/H/C, set N from the final output byte's most-significant bit, and repeat the corresponding transfer until B is zero | I/O Instructions, Table 46, pp. 232–234, notes 5–6 | 2026-07-20 |
| SLP is ED76, enters sleep, and does not affect flags | Special Control Instructions, Table 47, p. 235; ED op-code map, Table 50, p. 250 | 2026-07-20 |
| CBAR bits 7–4 are CA and bits 3–0 are BA; CBR and BBR are 8-bit 4 KiB physical bases; reset values are CBAR=F0h and CBR=BBR=00h | MMU, CBAR/CBR/BBR register descriptions, pp. 60–63 | 2026-07-20 |
| Logical pages below BA are Common Area 0 with physical base zero, pages from BA through the page before CA are Bank Area using BBR, and pages from CA upward are Common Area 1 using CBR; the selected 8-bit base is added to logical address bits 15–12 and bits 11–0 pass through | MMU, Figures 27–30, pp. 60–64 | 2026-07-20 |
| A write to CBR, BBR, or CBAR affects address translation beginning with the machine cycle immediately following the internal-I/O write cycle | MMU, CBR/BBR/CBAR register descriptions, pp. 61–63 | 2026-07-21 |
| ICR bits 7–6 relocate the 64-byte internal I/O window and reset to 00b, selecting addresses 0000h–003Fh; reset-state external Phase 1 ports therefore begin at 0040h | I/O Addressing, ICR and Figure 22, pp. 42–44 | 2026-07-20 |
| ITC bit 7 is TRAP, set only by an undefined opcode fetch and clearable by writing zero; bit 6 is read-only UFO; bits 2–0 are ITE2–ITE0; reset ITC is 01h | Interrupts, ITC register, pp. 67–68 | 2026-07-20 |
| Interrupt priority is TRAP, NMI, INT0, INT1, INT2, PRT0, PRT1, DMA0, DMA1, CSI/O, ASCI0, ASCI1 from highest to lowest; this corrects the plan's former placement of DMA0 before PRT1 | Interrupts, Figure 31 and Table 9, pp. 65, 82 | 2026-07-21 |
| NMI is edge-sensitive and latches an asserted edge until sampled; acceptance pushes PC, copies IEF1 to IEF2, clears IEF1, and vectors to logical 0066h. INT0, INT1, and INT2 are level-sensitive; ITE0/1/2 individually gate them and IEF1 globally gates all maskable sources | External Interrupts and Interrupt Sources During RESET, pp. 68–75, 81–83 | 2026-07-21 |
| INT1, INT2, and internal vectors combine I as bits 15–8, IL bits 7–5, and fixed low codes INT1=00h, INT2=02h, PRT0=04h, PRT1=06h, DMA0=08h, DMA1=0Ah, CSI/O=0Ch, ASCI0=0Eh, ASCI1=10h; the little-endian restart address is read from that logical table entry | Interrupt Vector Registers, Figure 41 and Table 9, pp. 66–67, 80–83 | 2026-07-21 |
| SLEEP ignores an interrupt whose individual enable is clear; NMI always exits SLEEP into its normal response; an individually enabled maskable source exits SLEEP into normal interrupt service when IEF1 is one, but with IEF1 zero resumes execution at the instruction following SLP without an acknowledge sequence | SLEEP Mode, pp. 33–35 | 2026-07-21 |
| API policy: `set_irq(..., true)` and `set_nmi(true)` mean logical assertion, matching qns `ASSERT=1`; the core translates that logical state to the UM's active-low, level-sensitive INT inputs and edge-sensitive NMI behavior | UM0050 external-interrupt behavior, pp. 72–83; PLAN.md Appendix D qns compatibility surface | 2026-07-21 |
| Implementation choice: because the fixed public HostBus/API has no interrupt-acknowledge data callback, INT0 acknowledge data is FFh; Mode 0 therefore performs RST 38h as explicitly allowed by Phase 5 task 3, and Mode 2 uses I:FFh as its vector-table address | PLAN.md Phase 5 task 3 fixed Mode 0 choice; UM0050 Mode 0 and Mode 2 acquisition, pp. 75–80 | 2026-07-21 |
| TRAP is non-maskable, leaves IEF1 and IEF2 unchanged, stacks the current PC high byte at SP-1 and low byte at SP-2, and vectors to logical 0000h | Interrupts, Table 8 and TRAP Interrupt, pp. 69–72 | 2026-07-20 |
| For a second-opcode undefined fetch, UFO is zero and the invalid instruction begins at stacked PC minus one; for a third-opcode undefined fetch, UFO is one and it begins at stacked PC minus two | Interrupts, ITC UFO description and TRAP Interrupt, pp. 68, 70–72 | 2026-07-20 |
| Z80180 instruction timing is the `States` column for every implemented form, including conditional taken/not-taken splits and terminal/repeating block-operation splits. Baseline Z80180 RETI uses the 22-state refetch sequence; the 12-state R0/R1 form applies to the later variant | Instruction summary, Tables 38–47, pp. 211–235; RETI Instruction Sequence and Table 10, pp. 84–85 | 2026-07-21 |
| Interrupt response totals are NMI 11 states, INT0 Mode 0 with the shown RST response 13, INT0 Mode 1 11, and vectored INT0 Mode 2 / INT1 / INT2 / internal interrupts 18 | Interrupt timing, Figures 35, 36, 38, 40, and 43, pp. 74, 76, 78, 80, and 86 | 2026-07-21 |
| Counting the complete instruction from its first prefix fetch through PC stacking, a second-opcode undefined TRAP consumes 17 states and a third-opcode undefined TRAP consumes 23 states | TRAP timing, Figures 32–33, pp. 71–72 | 2026-07-21 |
| DCNTL at internal I/O address 32h uses bits 7–6 for 0–3 memory waits and bits 5–4 for 1–4 external-I/O waits. Reset is F0h: the Wait Input and RESET section and both field descriptions explicitly set all four wait bits to 1, selecting three memory and four external-I/O waits; the contradictory all-zero reset row in the register table is treated as an erratum. Each HALT-mode idle cycle performs a memory read at the address following HALT and therefore receives the programmed memory waits | Wait State Generator, Figures 19–20 and Tables 3–4, pp. 28–33; DMA/WAIT Control Register, pp. 100–101 | 2026-07-21 |
| Internal I/O occupies one 64-byte window in the low 256 I/O addresses; ICR IOA7–6 select bases 00h/40h/80h/C0h, ICR bit 5 is IOSTP, all three bits reset to zero, and the high byte of a matching 16-bit internal-I/O address must be zero | Internal I/O Registers, ICR, Figure 22, and I/O Addressing Notes, pp. 41–44 | 2026-07-21 |
| Every internal-I/O write also performs the same write on the external bus, and every internal-I/O read performs the same external read but ignores its returned data; these duplicate cycles use internal-I/O timing and ignore WAIT and programmed external-I/O waits | I/O Addressing Notes, p. 44 | 2026-07-21 |
| Tables 6–7 define the complete Z80180 and Z8S180 64-byte register maps. Table 6 names address 0Bh `TRD`, not the plan's former `TRDR`; the controlling plan was corrected to the manufacturer mnemonic | Internal I/O Register Address Map, Tables 6–7, pp. 44–51 | 2026-07-21 |
| CNTLA0 resets to 10h and CNTLA1 to 00h when the indeterminate MPBR/EFR bit is deterministically chosen as zero; CNTLB0/1 reset to 07h when indeterminate MPBT is chosen as zero. STAT0/1 reset with TDRE set (02h), following the field descriptions rather than the contradictory all-zero reset rows | ASCI register descriptions, pp. 118–134 | 2026-07-21 |
| CNTR resets to 07h; EF is read-only, bit 3 is not implemented, and EIE/RE/TE/SS2–0 are writable. TRD is the 8-bit read/write CSI/O transmit/receive register | CSI/O register descriptions, pp. 147–149 | 2026-07-21 |
| TMDR0/1 and RLDR0/1 reset to FFFFh according to the repeated PRT and RESET prose, overriding the contradictory zero rows in the byte-register diagrams; TCR resets to zero with TIF1–0 read-only and its low six control bits writable | PRT register descriptions and PRT and RESET, pp. 157–164 | 2026-07-21 |
| Both PRT channels use the system clock divided by 20. While TDEn is set, nonzero TMDRn decrements each timer tick; reaching zero sets TIFn, zero remains observable for that tick interval, and the following timer tick reloads TMDRn from RLDRn | PRT Block Diagram, Timer Data Register, and Figure 64, pp. 157–163 | 2026-07-21 |
| A low-byte TMDRn read captures the simultaneous high byte for the following high-byte read; TMDRn writes require TDEn clear. TIFn clears only after TCR is read and either TMDRn byte is then read | Timer Data Register and TCR TIF descriptions, pp. 157–162 | 2026-07-21 |
| PRTn raises its qualified internal request while TIFn and TIEn are both one; the interrupt controller separately applies IEF1. PRT0 has priority over PRT1 | TCR TIE descriptions and Figure 66, pp. 162–164; Interrupts Figure 31 and Table 9, pp. 65, 82 | 2026-07-21 |
| FRC is an 8-bit read-only free-running down-counter initialized to FFh during RESET; it decrements once every ten phi clocks, wraps from 00h to FFh, reads do not affect it, and it continues in I/O STOP. UM0050 omits the rate; the original HD64180 manual states it explicitly | Free Running Counter, p. 172; Hitachi HD64180 User's Manual, §2.15, p. 96 | 2026-07-21 |
| Each ASCI channel has separate TDR/TSR and RSR/RDR stages. TDR transfers to TSR when TSR is empty; TDRE clears while TDR is full and sets again after that transfer. A completed receive byte sets RDRF, reading RDR clears or advances it, and a full RDR when RSR completes sets OVRN without replacing the prior byte | ASCI block diagram and TDR/RDR/STAT descriptions, pp. 117–122 | 2026-07-21 |
| An ASCI frame comprises one start bit, seven or eight data bits, an optional parity bit or multiprocessor bit, and one or two stop bits. Standard internal bit time is `(10 + 20*PS) * 2^SS * (16 or 64)` phi clocks; SS=7 selects external CKA | CNTLA/CNTLB descriptions and ASCI Baud Rate Selection, pp. 123–134, 141–145 | 2026-07-21 |
| RIE qualifies RDRF/OVRN/PE/FE and the ASCI0 DCD low-to-high transition; TIE qualifies TDRE. DCD high resets and inhibits channel 0 receive, and its high indication persists through the first STAT0 read after a high-to-low transition. CTS high suppresses TDRE but does not stop transmission already in progress; CTS1 gating requires CTS1E | STAT0/STAT1 and modem-control descriptions, pp. 120–124, 132–134 | 2026-07-21 |
| Z8S180 ASCI receive is four-byte FIFO buffered. ASEXT supplies RDRF interrupt inhibit, DCD0/CTS0 advisory disables, X1 bit-clock, and 16-bit BRG mode; BRG bit time is `2 * (ASTC + 2) * clock_mode` phi clocks | ASEXT/ASTC and Z8S180 receive/BRG descriptions, pp. 135–146 | 2026-07-21 |
| RESET and I/O STOP stop ASCI operation, clear RE/TE and receive/error flags, and set TDRE, while TDR/RDR data contents are unchanged | ASCI and RESET / I/O STOP, p. 141 | 2026-07-21 |
| Implementation boundary: SS=7 frames wait in the shift stage because the fixed host API has no external CKA edge source; z-core does not infer an external frequency or add an unplanned clock callback | UM0050 external-clock selection, pp. 142–145; PLAN.md fixed Phase 1 public API | 2026-07-21 |
| CSI/O CNTR implements EF/EIE/RE/TE and SS2-0. EF sets after one 8-bit transmit or receive, EF and EIE qualify the interrupt request, TRD/TRDR access clears EF, and software clearing RE or TE immediately aborts the active operation | CSI/O CNTR and operation descriptions, pp. 147–152 | 2026-07-21 |
| CSI/O is unbuffered and half-duplex: RE and TE operations cannot run simultaneously, a TRD/TRDR write during transmit immediately changes shift data, completion automatically clears the active enable, and CTS1E must be zero for RXS to own the multiplexed pin | CSI/O CNTR, TRDR, and operation notes, pp. 148–152 | 2026-07-21 |
| CSI/O internal clock bit times are phi divided by 20, 40, 80, 160, 320, 640, and 1280 for SS=0 through 6; SS=7 selects external CKS | CSI/O Baud Rate Selection, Table 22, p. 150 | 2026-07-21 |
| RESET aborts CSI/O, restores CNTR to 07h, and preserves TRD/TRDR contents. I/O STOP clears EF/RE/TE and aborts transfer while EIE and TRD/TRDR remain unchanged | CNTR field descriptions and CSI/O and RESET, pp. 148–153 | 2026-07-21 |
| Implementation boundary: SS=7 CSI/O remains active because the fixed host API has no external CKS edge source; z-core does not infer an external frequency or add an unplanned clock callback | UM0050 Table 22 and external-clock timing, pp. 150, 154, 156; PLAN.md fixed Phase 1 public API | 2026-07-21 |
| DMA 20-bit address registers use full low/high bytes plus the low nibble of their bank byte; DMODE implements bits 5–1; DSTAT reads DWE1–0 as one, uses active-low DWE1–0 to gate software writes to DE1–0, sets DME when a gated DE bit is written one, and leaves DME software-read-only | DMA register descriptions, pp. 93–102 | 2026-07-21 |
| DMA0 supports incrementing/decrementing memory-to-memory, fixed memory-mapped-I/O, and fixed true-I/O endpoints; MMOD selects burst or cycle steal only for memory-to-memory. DMA1's DIM bits select memory-to-I/O or I/O-to-memory and increment or decrement MAR1 while IAR1 remains fixed | DMODE/DCNTL and transfer-mode tables, pp. 98–104 | 2026-07-21 |
| Memory-to-memory DMA0 ignores DREQ0. I/O and memory-mapped-I/O transfers are controlled by active-low DREQ; level sense continues while asserted, edge sense permits one byte per falling edge, simultaneous channel requests prefer DMA0, and a DMA0 memory-to-memory operation excludes DMA1 until DMA0 terminates | DMA operation and channel priority, pp. 105–113 | 2026-07-21 |
| Each DMA byte uses a three-clock read plus a three-clock write. DCNTL memory waits apply to each memory access; true I/O also has its mandatory one wait plus the programmed I/O waits, while memory-mapped I/O uses memory timing. An A15/A16 carry or borrow extends an otherwise three-clock affected memory cycle to four clocks | DMA timing and bus timing, pp. 91, 105–113 | 2026-07-21 |
| DMA completion clears the channel DE bit and `!DE && DIE` is a level interrupt request. NMI clears only DME so rewriting an enabled DE with matching DWE low restarts the suspended operation. RESET stops DMA and resets its control registers while preserving address and byte-count contents | DMAC Internal Interrupts, DMAC and NMI, and DMAC and RESET, pp. 114–115 | 2026-07-21 |
| API/timing boundary: `set_dreq(ch, true)` means logical assertion of the UM's active-low pin. DMA interleaves at complete-byte/instruction boundaries: burst drains before the next instruction, cycle steal and edge sense transfer one byte per CPU instruction boundary, and no read/write half-cycle suspension callback is invented | PLAN.md fixed DMA API and Phase 4 timing-model boundary; UM0050 pp. 105–115 | 2026-07-21 |
| Real peripheral requests arbitrate in the fixed adjacent order PRT0 > PRT1 > DMA0 > DMA1 > CSI/O > ASCI0 > ASCI1. Each owner raises and clears its documented level request, and each selected source vectors through its own I:IL table entry with the 18-cycle internal acknowledge | Interrupts Figure 31 and Table 9, pp. 65, 82; PRT, DMA, CSI/O, and ASCI request descriptions; P6.6 pairwise integration authority | 2026-07-21 |
| Save-state boundary: the versioned postcard payload contains all core-owned machine, memory, timing, peripheral, queue, and event state; the generic HostBus remains host-owned, and the derived MMU page cache is recomputed after atomic load | PLAN.md save-state API and Phase 6 task 7; P6.7 round-trip property authority | 2026-07-21 |
| Determinism authority: two independently constructed PRT + ASCI + DMA machines each consume exactly 10,000,000 phi cycles and produce byte-identical versioned states plus identical nonempty event streams | PLAN.md Phase 6 task 8; P6.8 scripted determinism authority | 2026-07-21 |
| IL implements bits 7–5 and resets to zero; ITC reads TRAP/UFO/ITE2–0, lets software clear but not set TRAP, keeps UFO read-only, and resets to 01h; RCR implements bits 7–6 and 1–0 and resets to C0h | IL/ITC and RCR register descriptions, pp. 67–68, 88 | 2026-07-21 |
| OMCR resets to E0h, implements M1E/IOC as read/write and M1TE as write-only; CMR is Z8S180-only with writable X2 at bit 7 and reset-one reserved bits 6–0; CCR is Z8S180-only, all eight bits are read/write, and resets to zero | Operation Modes, p. 15; CMR/CCR, pp. 52–53 | 2026-07-21 |
| Z8S180 adds ASEXT0/1 at 12h/13h, four ASCI time-constant bytes at 1Ah–1Dh, CMR/CCR at 1Eh/1Fh, and IAR1B at 2Dh; IAR1B implements bits 7–6 and 3–0 while reserved bits 5–4 must remain zero | Internal I/O Register Address Map, Table 7, pp. 48–51; extension-register descriptions, pp. 102, 135–138 | 2026-07-21 |
| Free choice: wholly reserved internal-I/O addresses read zero and ignore writes; register bits whose reset state UM0050 marks indeterminate, and DMA storage registers for which it supplies no reset value, initialize to zero for deterministic emulation | Tables 6–7 and referenced register descriptions leave these values unspecified; z-core deterministic policy | 2026-07-21 |
| Debug-event API boundary: a configurable newest-retained ring records watched CPU/DMA physical memory accesses, traced CPU/DMA I/O accesses, traced interrupt acknowledgements, traps, and ROM write attempts; host debugger memory access is excluded, and reset preserves producer configuration while clearing observations | PLAN.md section 2.2 and Phase 7 task 1; P7.1 API authority | 2026-07-21 |
| Instruction-trace API boundary: each executed instruction, including an undefined-opcode TRAP, records its entry cycle, logical and translated physical PC, and actually fetched bytes without extra bus reads; DMA, interrupt acknowledge, and idle calls are excluded | PLAN.md section 2.2 and Phase 7 task 2; P7.2 API authority | 2026-07-21 |
| Disassembler provenance boundary: all implemented main, CB, ED, DD, FD, DDCB, and FDCB encodings format from the single optable metadata; arbitrary undefined or truncated byte streams remain total and tile through bounded data records | PLAN.md section 2.3 and Phase 7 task 3; P7.3 all-table and proptest authorities | 2026-07-21 |
| Bare-ROM runner boundary: the strict TOML config maps clock, variant, and exactly one image-backed ROM plus RAM/external regions directly into the core machine configuration; bounded execution uses core stepping, traces use the core instruction ring and disassembler, and trap or no-progress termination is explicit | PLAN.md Phase 7 task 4; P7.4 config, trace, trap, and command-process authorities | 2026-07-21 |
